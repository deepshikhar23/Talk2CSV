# README.md

# Conversational CSV Data Analyst

This is an AI agent that can understand and answer natural language questions about a provided CSV file. It uses the LangChain framework and Google's Gemini model to write and execute Python (pandas) code to find answers from the data.